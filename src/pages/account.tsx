import { Account } from "@/page-components/account";
import { Layout } from "@/widgets/layout";

export default function AccountPage() {
  return (
    <Layout headTitle="Account" title="Account page Gscore">
      <Account />
    </Layout>
  );
}
